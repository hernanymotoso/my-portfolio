import React from 'react';

import { Container, ScrollBtn } from './styles';

const ScrollDownButton: React.FC = () => {
  console.log();

  return (
    <Container>
      <ScrollBtn />

      <p>Scroll</p>
    </Container>
  );
};

export default ScrollDownButton;
